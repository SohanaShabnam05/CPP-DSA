#include <iostream>

using namespace std;

bool areAnagram(string &S1, string &S2)
{
    if(S1.length() != S2.length())
    {
        return false;
    }
    sort(S1.begin(),S1.end());
}

int main(){
    
    return 0;
}